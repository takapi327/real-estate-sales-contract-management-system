# https://www.playframework.com/documentation/latest/Configuration

database {
  driver = "slick.driver.MySQLDriver$"
  db {
    profile  = "com.mysql.Driver"
    user     = "root"
    url      = "jdbc:mysql://localhost/contract_management_system"
    password = "transamGN001"
  }
}
